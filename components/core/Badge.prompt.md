Small status pill. Used in the sidebar (case status), the document card header (e.g. "Draft"), and the empty-state chips.

```jsx
<Badge tone="brand">Draft</Badge>
<Badge tone="warn" icon={<Icon name="clock" size={12} />}>Awaiting reply · 18 days</Badge>
```

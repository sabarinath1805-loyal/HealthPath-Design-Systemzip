Icon-only button. Used in the composer (mic, attachment, send), sidebar header (collapse/menu), and message-action rows. `label` is required for screen readers.

```jsx
<IconButton label="Voice input"><Icon name="mic" /></IconButton>
<IconButton label="Send" variant="brand" round><Icon name="arrowUp" /></IconButton>
```
